Cosas a tener en cuenta:

- Validación variable para evitar que entren directamente sin haber pasado por login.
- Saneamiento de variables que vienen de otra pagina para poder usarlas en consultas base de datos.
- Usar estatements para las consultas, nunca poner directamente el valor en la consulta.
- Deshabilitar el autocomit y commitear nosotros mismos los statements.
- Cerrar estatements, sessiones?, conexiones cuando terminemos de usarlas.
- Hacer un rollback si no ha funcionado como queriamos (solo en los casos que haya más de una consulta).
- Utilizar try catch finally para manejar errores.
- Enviar mensajes de error o éxito al usuario. (sin especificar que es lo que ha fallado)
- No mostrar detalles del servidor ni información de configuración.
- Todo el código PHP debe estar correctamentel indentado.
- Evitar repetir mismo código.
- Evitar usar programación orientada a objetos, ya que no lo hemos aprendido en clase ( -> )
- Uso de constantes para valores fijos.
- Uso de funciones para reutilizar codigo.
- Separar HTML del PHP.
- Crear carpeta para codigo php que no se muestra al usuario. (como hacemos con css o src)
- Uso de UTF8 en todo el proyecto.
- Comentarios claros en el codigo. (Para poder identificar donde esta cada cosa y poder explicar que hace cada punto si no nos acordamos)

En las validaciones php crear las variables de session (IMPORTANTE)

Estructura de carpetas (a debatir):
/sql para .sql que creemos para nosotros (para crear la base de datos y los inserts)
/controller para PHP
/view para HTML (junto con PHP que tenga)
/src/css para CSS 
/src/images para imagenes